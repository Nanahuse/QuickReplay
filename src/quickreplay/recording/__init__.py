"""Recording domain models and the worker command / event protocol.

Value models (segments, sessions, metrics) and the messages exchanged with the
recorder worker process.  Nothing in this package performs real recording.
"""
