"""Application state and view models.

This package holds the UI-independent application state machine and the
read-only view state that a future UI layer will render.  It must not import
any native or UI library.
"""
